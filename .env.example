# Server-side only. Never put keys in evidence, public env names or CLI arguments.
# Jupiter key is optional; Ondo key is optional when NVDAon is transparently excluded.
# Pyth is optional. Without a key, Yahoo chart supplies a labeled reference.
# Open-session stale/missing confidence fails; qualified last-market anchors have explicit closed-session semantics.
JUPITER_API_KEY=
ONDO_API_KEY=
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
PYTH_HERMES_URL=https://pyth.dourolabs.app/hermes
PYTH_API_KEY=
